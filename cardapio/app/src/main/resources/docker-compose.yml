services:
  mysql:
    image: mysql:latest
    container_name: cardapio-mysql
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: cardapio
      MYSQL_ROOT_PASSWORD: root123456
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
